# IMPORTANT NOTE
The '.htaccess' file must have the SITELOADNAME set to get the SimpleSiteClass!

This site uses SimpleSiteClass. The 'includes' directory must have the check at the start of each file for SimpleDatabase NOT Database!
