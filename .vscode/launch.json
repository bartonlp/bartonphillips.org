{
    // Use IntelliSense to learn about possible attributes.
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/bartonphillips.org": "${workspaceFolder}" // Adjust this to your project's root
            },
            "log": true
        }
    ]
}